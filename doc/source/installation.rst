============
Installation
============

At the command line::

    $ pip install openstack-check

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv openstack-check
    $ pip install openstack-check
